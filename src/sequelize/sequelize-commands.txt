# link - https://sequelize.org/master/manual/migrations.html

# copy

.sequelizerc file

# run these commands

npx sequelize-cli init

npx sequelize-cli model:generate --name Employees --attributes first_name:string,last_name:string,age:integer,address:string,role:string,department:string,position:string

npx sequelize-cli db:migrate

npx sequelize seed:generate --name employees-seeder

npx sequelize db:seed:all

npx sequelize db:seed --seed 20240214012345-demo-user.js
